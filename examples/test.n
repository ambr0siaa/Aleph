function main():int
    a:float = 213.0
    b:float = 245.0
    t:float = 0.56
    lerp:function = (x, y, t:float):int
                        return x + (y - x) * t
    if a * b >= 1234:
        print("Hello, world\n")
    else
        print("%d\n", lerp(a, b, t))
    end
    return 0
end
